{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Notifications list</h1>

    <table class="records_list" border="1">
        <thead>
            <tr>
                <th>Datecreation</th>
                <th>Contenu</th>
                <th>Type</th>
                <th>Lu</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
               {# <td><a href="{{ path('notifications_show', { 'id': entity.id }) }}">{{ entity.dateCreation }}</a></td>#}
                <td>{{ entity.dateCreation|date('H:i:s')}}</td>
                <td>{{ entity.contenu }}</td>
                <td>{{ entity.type }}</td>
                <td>{{ entity.lu }}</td>
                <td>{{ entity.id }}</td>
                <td>
              {#  <ul>
                    <li>
                        <a href="{{ path('notifications_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('notifications_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>#}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {#     <ul>
        <li>
            <a href="{{ path('notifications_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>#}
    {% endblock %}
