{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Reservations</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Datereservation</th>
                <td>{{ entity.dateReservation|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ entity.etat }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('reservations') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('reservations_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
